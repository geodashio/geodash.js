var assert = require('assert');
var geodash = require("./../../geodash");

/*
describe("Testing geodash.util.clearValue(keyChain, target)", function(){

  var y = {'a' : {'b': undefined}};

  it("with string", function(){
    assert.deepEqual(geodash.util.clearValue('a__b', {'a' : {'b': 'x'}}), y);
  });

  it("with object", function(){
    assert.deepEqual(geodash.util.clearValue(['a','b'], {'a' : {'b': 'x'}}), y);
  });

});
*/
