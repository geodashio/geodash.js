'use strict';

/**
 * Bootloader functions for GeoDash
 * @namespace loaders
 * @memberof geodash.bootloader
 */

module.exports = {
  endpoints: require("./endpoints"),
  pages: require("./pages")
};
